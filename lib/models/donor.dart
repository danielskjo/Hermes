class Donor {
  String id;
  String name;
  String username;
  String email;
  String password;
  String image;
  String address;
  // DateTime date;

  Donor(this.id, this.name, this.username, this.email, this.password, this.image, this.address);
}