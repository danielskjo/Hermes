class Student {
  String id;
  String name;
  String username;
  String email;
  String password;
  String image;
  String university;
  // DateTime date;

  Student(this.id, this.name, this.username, this.email, this.password, this.image, this.university);
}